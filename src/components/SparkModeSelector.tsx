// ✅ DEPRECATED: Spark features (Lazy Edits, Smart Context) have been removed
// These features depended on the Applaa gateway infrastructure which is no longer used
// Pro users now use their own API keys directly with providers

// This component is kept for backwards compatibility but returns null to hide the UI
export function SparkModeSelector() {
  return null;
}
